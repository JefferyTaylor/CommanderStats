// Copy this file to config.js and fill values in.
// Do not commit real keys if your repo is public.
window.APP_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: "",

  // Optional shared PIN to reduce accidental edits.
  // NOTE: This is client-side only and NOT secure against determined attackers.
  recorderPin: "",
};
